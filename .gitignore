node_modules
dist
.DS_Store
*.local
card-data/source-recovered
.worktrees

# Card Studio working photos — large binaries, not design decisions.
# Each set's crops.json and team-overrides.json stay TRACKED.
card-art/sets/*/photos
