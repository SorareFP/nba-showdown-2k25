// Entry for studio-export.html — the batch export's single-card page.
import React from 'react';
import { createRoot } from 'react-dom/client';
import ExportFrame from './ExportFrame.jsx';

createRoot(document.getElementById('root')).render(<ExportFrame />);
